// step1: const favorites = JSON.parse(localStorage.getItem('favorites'))

// step 2: check value of favorites if is it null

// step 3 : if its null : do nothing

// step 4 : if its not null (it will be array of ids): 
